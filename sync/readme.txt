readme for sync

use this config instead setwgx.github.io/.git/config file

.git is a hidden folder

then, setting the global config

1 git config --global user.email "mccarty_zzz2017@163.com"
2 git config --global user.name "setwgx"

git add .
git commit -m "something"

finally, use "git push github/gitee master" to upload your files.
